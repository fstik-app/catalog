diff --git a/server/docker-compose.yml b/server/docker-compose.yml
index 1d0e02b..038761a 100644
--- a/server/docker-compose.yml
+++ b/server/docker-compose.yml
@@ -1,5 +1,3 @@
-version: '3.8'
-
 services:
   api:
     build: .
@@ -23,7 +21,7 @@ services:
     image: mongo:6
     restart: unless-stopped
     volumes:
-      - mongodb_data:/data/db
+      - ./data/mongodb:/data/db
     environment:
       - MONGO_INITDB_DATABASE=fstik
     networks:
@@ -33,14 +31,22 @@ services:
     image: redis:7-alpine
     restart: unless-stopped
     volumes:
-      - redis_data:/data
+      - ./data/redis:/data
     command: redis-server --appendonly yes
     networks:
       - default

-volumes:
-  mongodb_data:
-  redis_data:
+  restore:
+    image: mongo:6
+    depends_on:
+      - mongodb
+    volumes:
+      - ./dump:/dump
+    command: mongorestore --host=mongodb --db=fstik /dump/fstik
+    networks:
+      - default
+    profiles:
+      - restore

 networks:
   traefik-public:
